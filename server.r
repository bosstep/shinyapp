library(shiny)
library(datasets)

# class example 001 server.R

# define server logic to plot various variables against mpg
# empty now, but server.R is always required

shinyServer(function(input, output) {
})
